{
  "name": "drivemarket",
  "version": "1.0.0",
  "description": "DriveMarket PHP application",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@vercel/php": "^0.6.0"
  }
}
